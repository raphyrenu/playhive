// components/Footer.tsx

import React from 'react';
import { FaTwitter, FaGithub, FaLinkedin } from 'react-icons/fa'; // You can install react-icons if not already installed

const Footer: React.FC = ({currentTheme}) => {
  return (
     
  );
};

export default Footer;
